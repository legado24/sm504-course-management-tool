// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package tr.edu.metu.ii.sm504.domain;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;
import tr.edu.metu.ii.sm504.domain.ScheduleTemplate;
import tr.edu.metu.ii.sm504.domain.ScheduleTemplateDataOnDemand;
import tr.edu.metu.ii.sm504.domain.User;
import tr.edu.metu.ii.sm504.domain.UserDataOnDemand;

privileged aspect ScheduleTemplateDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ScheduleTemplateDataOnDemand: @Component;
    
    private Random ScheduleTemplateDataOnDemand.rnd = new SecureRandom();
    
    private List<ScheduleTemplate> ScheduleTemplateDataOnDemand.data;
    
    @Autowired
    private UserDataOnDemand ScheduleTemplateDataOnDemand.userDataOnDemand;
    
    public ScheduleTemplate ScheduleTemplateDataOnDemand.getNewTransientScheduleTemplate(int index) {
        ScheduleTemplate obj = new ScheduleTemplate();
        setCreatedBy(obj, index);
        setCreationTime(obj, index);
        setName(obj, index);
        setUpdateTime(obj, index);
        setUpdatedBy(obj, index);
        return obj;
    }
    
    public void ScheduleTemplateDataOnDemand.setCreatedBy(ScheduleTemplate obj, int index) {
        User createdBy = userDataOnDemand.getRandomUser();
        obj.setCreatedBy(createdBy);
    }
    
    public void ScheduleTemplateDataOnDemand.setCreationTime(ScheduleTemplate obj, int index) {
        Date creationTime = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setCreationTime(creationTime);
    }
    
    public void ScheduleTemplateDataOnDemand.setName(ScheduleTemplate obj, int index) {
        String name = "name_" + index;
        obj.setName(name);
    }
    
    public void ScheduleTemplateDataOnDemand.setUpdateTime(ScheduleTemplate obj, int index) {
        Date updateTime = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setUpdateTime(updateTime);
    }
    
    public void ScheduleTemplateDataOnDemand.setUpdatedBy(ScheduleTemplate obj, int index) {
        User updatedBy = userDataOnDemand.getRandomUser();
        obj.setUpdatedBy(updatedBy);
    }
    
    public ScheduleTemplate ScheduleTemplateDataOnDemand.getSpecificScheduleTemplate(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        ScheduleTemplate obj = data.get(index);
        Long id = obj.getId();
        return ScheduleTemplate.findScheduleTemplate(id);
    }
    
    public ScheduleTemplate ScheduleTemplateDataOnDemand.getRandomScheduleTemplate() {
        init();
        ScheduleTemplate obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return ScheduleTemplate.findScheduleTemplate(id);
    }
    
    public boolean ScheduleTemplateDataOnDemand.modifyScheduleTemplate(ScheduleTemplate obj) {
        return false;
    }
    
    public void ScheduleTemplateDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = ScheduleTemplate.findScheduleTemplateEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'ScheduleTemplate' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<ScheduleTemplate>();
        for (int i = 0; i < 10; i++) {
            ScheduleTemplate obj = getNewTransientScheduleTemplate(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
