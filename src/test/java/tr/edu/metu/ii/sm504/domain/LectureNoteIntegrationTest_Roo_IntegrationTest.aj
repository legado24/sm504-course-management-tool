// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package tr.edu.metu.ii.sm504.domain;

import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;
import tr.edu.metu.ii.sm504.domain.LectureNote;
import tr.edu.metu.ii.sm504.domain.LectureNoteDataOnDemand;
import tr.edu.metu.ii.sm504.domain.LectureNoteIntegrationTest;

privileged aspect LectureNoteIntegrationTest_Roo_IntegrationTest {
    
    declare @type: LectureNoteIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: LectureNoteIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: LectureNoteIntegrationTest: @Transactional;
    
    @Autowired
    private LectureNoteDataOnDemand LectureNoteIntegrationTest.dod;
    
    @Test
    public void LectureNoteIntegrationTest.testCountLectureNotes() {
        Assert.assertNotNull("Data on demand for 'LectureNote' failed to initialize correctly", dod.getRandomLectureNote());
        long count = LectureNote.countLectureNotes();
        Assert.assertTrue("Counter for 'LectureNote' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void LectureNoteIntegrationTest.testFindLectureNote() {
        LectureNote obj = dod.getRandomLectureNote();
        Assert.assertNotNull("Data on demand for 'LectureNote' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'LectureNote' failed to provide an identifier", id);
        obj = LectureNote.findLectureNote(id);
        Assert.assertNotNull("Find method for 'LectureNote' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'LectureNote' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void LectureNoteIntegrationTest.testFindAllLectureNotes() {
        Assert.assertNotNull("Data on demand for 'LectureNote' failed to initialize correctly", dod.getRandomLectureNote());
        long count = LectureNote.countLectureNotes();
        Assert.assertTrue("Too expensive to perform a find all test for 'LectureNote', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<LectureNote> result = LectureNote.findAllLectureNotes();
        Assert.assertNotNull("Find all method for 'LectureNote' illegally returned null", result);
        Assert.assertTrue("Find all method for 'LectureNote' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void LectureNoteIntegrationTest.testFindLectureNoteEntries() {
        Assert.assertNotNull("Data on demand for 'LectureNote' failed to initialize correctly", dod.getRandomLectureNote());
        long count = LectureNote.countLectureNotes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<LectureNote> result = LectureNote.findLectureNoteEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'LectureNote' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'LectureNote' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void LectureNoteIntegrationTest.testFlush() {
        LectureNote obj = dod.getRandomLectureNote();
        Assert.assertNotNull("Data on demand for 'LectureNote' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'LectureNote' failed to provide an identifier", id);
        obj = LectureNote.findLectureNote(id);
        Assert.assertNotNull("Find method for 'LectureNote' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyLectureNote(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'LectureNote' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void LectureNoteIntegrationTest.testMergeUpdate() {
        LectureNote obj = dod.getRandomLectureNote();
        Assert.assertNotNull("Data on demand for 'LectureNote' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'LectureNote' failed to provide an identifier", id);
        obj = LectureNote.findLectureNote(id);
        boolean modified =  dod.modifyLectureNote(obj);
        Integer currentVersion = obj.getVersion();
        LectureNote merged = (LectureNote)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'LectureNote' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void LectureNoteIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'LectureNote' failed to initialize correctly", dod.getRandomLectureNote());
        LectureNote obj = dod.getNewTransientLectureNote(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'LectureNote' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'LectureNote' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'LectureNote' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void LectureNoteIntegrationTest.testRemove() {
        LectureNote obj = dod.getRandomLectureNote();
        Assert.assertNotNull("Data on demand for 'LectureNote' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'LectureNote' failed to provide an identifier", id);
        obj = LectureNote.findLectureNote(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'LectureNote' with identifier '" + id + "'", LectureNote.findLectureNote(id));
    }
    
}
