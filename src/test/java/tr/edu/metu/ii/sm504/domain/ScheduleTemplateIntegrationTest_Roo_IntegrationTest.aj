// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package tr.edu.metu.ii.sm504.domain;

import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;
import tr.edu.metu.ii.sm504.domain.ScheduleTemplate;
import tr.edu.metu.ii.sm504.domain.ScheduleTemplateDataOnDemand;
import tr.edu.metu.ii.sm504.domain.ScheduleTemplateIntegrationTest;

privileged aspect ScheduleTemplateIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ScheduleTemplateIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ScheduleTemplateIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ScheduleTemplateIntegrationTest: @Transactional;
    
    @Autowired
    private ScheduleTemplateDataOnDemand ScheduleTemplateIntegrationTest.dod;
    
    @Test
    public void ScheduleTemplateIntegrationTest.testCountScheduleTemplates() {
        Assert.assertNotNull("Data on demand for 'ScheduleTemplate' failed to initialize correctly", dod.getRandomScheduleTemplate());
        long count = ScheduleTemplate.countScheduleTemplates();
        Assert.assertTrue("Counter for 'ScheduleTemplate' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ScheduleTemplateIntegrationTest.testFindScheduleTemplate() {
        ScheduleTemplate obj = dod.getRandomScheduleTemplate();
        Assert.assertNotNull("Data on demand for 'ScheduleTemplate' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ScheduleTemplate' failed to provide an identifier", id);
        obj = ScheduleTemplate.findScheduleTemplate(id);
        Assert.assertNotNull("Find method for 'ScheduleTemplate' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ScheduleTemplate' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ScheduleTemplateIntegrationTest.testFindAllScheduleTemplates() {
        Assert.assertNotNull("Data on demand for 'ScheduleTemplate' failed to initialize correctly", dod.getRandomScheduleTemplate());
        long count = ScheduleTemplate.countScheduleTemplates();
        Assert.assertTrue("Too expensive to perform a find all test for 'ScheduleTemplate', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ScheduleTemplate> result = ScheduleTemplate.findAllScheduleTemplates();
        Assert.assertNotNull("Find all method for 'ScheduleTemplate' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ScheduleTemplate' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ScheduleTemplateIntegrationTest.testFindScheduleTemplateEntries() {
        Assert.assertNotNull("Data on demand for 'ScheduleTemplate' failed to initialize correctly", dod.getRandomScheduleTemplate());
        long count = ScheduleTemplate.countScheduleTemplates();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ScheduleTemplate> result = ScheduleTemplate.findScheduleTemplateEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ScheduleTemplate' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ScheduleTemplate' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ScheduleTemplateIntegrationTest.testFlush() {
        ScheduleTemplate obj = dod.getRandomScheduleTemplate();
        Assert.assertNotNull("Data on demand for 'ScheduleTemplate' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ScheduleTemplate' failed to provide an identifier", id);
        obj = ScheduleTemplate.findScheduleTemplate(id);
        Assert.assertNotNull("Find method for 'ScheduleTemplate' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyScheduleTemplate(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ScheduleTemplate' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ScheduleTemplateIntegrationTest.testMergeUpdate() {
        ScheduleTemplate obj = dod.getRandomScheduleTemplate();
        Assert.assertNotNull("Data on demand for 'ScheduleTemplate' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ScheduleTemplate' failed to provide an identifier", id);
        obj = ScheduleTemplate.findScheduleTemplate(id);
        boolean modified =  dod.modifyScheduleTemplate(obj);
        Integer currentVersion = obj.getVersion();
        ScheduleTemplate merged = (ScheduleTemplate)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ScheduleTemplate' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ScheduleTemplateIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ScheduleTemplate' failed to initialize correctly", dod.getRandomScheduleTemplate());
        ScheduleTemplate obj = dod.getNewTransientScheduleTemplate(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ScheduleTemplate' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ScheduleTemplate' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'ScheduleTemplate' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ScheduleTemplateIntegrationTest.testRemove() {
        ScheduleTemplate obj = dod.getRandomScheduleTemplate();
        Assert.assertNotNull("Data on demand for 'ScheduleTemplate' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ScheduleTemplate' failed to provide an identifier", id);
        obj = ScheduleTemplate.findScheduleTemplate(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ScheduleTemplate' with identifier '" + id + "'", ScheduleTemplate.findScheduleTemplate(id));
    }
    
}
