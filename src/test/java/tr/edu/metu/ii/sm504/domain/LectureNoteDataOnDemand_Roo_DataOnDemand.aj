// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package tr.edu.metu.ii.sm504.domain;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;
import tr.edu.metu.ii.sm504.domain.Course;
import tr.edu.metu.ii.sm504.domain.CourseDataOnDemand;
import tr.edu.metu.ii.sm504.domain.LectureNote;
import tr.edu.metu.ii.sm504.domain.LectureNoteDataOnDemand;
import tr.edu.metu.ii.sm504.domain.User;
import tr.edu.metu.ii.sm504.domain.UserDataOnDemand;

privileged aspect LectureNoteDataOnDemand_Roo_DataOnDemand {
    
    declare @type: LectureNoteDataOnDemand: @Component;
    
    private Random LectureNoteDataOnDemand.rnd = new SecureRandom();
    
    private List<LectureNote> LectureNoteDataOnDemand.data;
    
    @Autowired
    private CourseDataOnDemand LectureNoteDataOnDemand.courseDataOnDemand;
    
    @Autowired
    private UserDataOnDemand LectureNoteDataOnDemand.userDataOnDemand;
    
    public LectureNote LectureNoteDataOnDemand.getNewTransientLectureNote(int index) {
        LectureNote obj = new LectureNote();
        setCourse(obj, index);
        setCreatedBy(obj, index);
        setCreationTime(obj, index);
        setData(obj, index);
        setName(obj, index);
        setUpdateTime(obj, index);
        setUpdatedBy(obj, index);
        return obj;
    }
    
    public void LectureNoteDataOnDemand.setCourse(LectureNote obj, int index) {
        Course course = courseDataOnDemand.getRandomCourse();
        obj.setCourse(course);
    }
    
    public void LectureNoteDataOnDemand.setCreatedBy(LectureNote obj, int index) {
        User createdBy = userDataOnDemand.getRandomUser();
        obj.setCreatedBy(createdBy);
    }
    
    public void LectureNoteDataOnDemand.setCreationTime(LectureNote obj, int index) {
        Date creationTime = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setCreationTime(creationTime);
    }
    
    public void LectureNoteDataOnDemand.setData(LectureNote obj, int index) {
        byte[] data = String.valueOf(index).getBytes();
        obj.setData(data);
    }
    
    public void LectureNoteDataOnDemand.setName(LectureNote obj, int index) {
        String name = "name_" + index;
        obj.setName(name);
    }
    
    public void LectureNoteDataOnDemand.setUpdateTime(LectureNote obj, int index) {
        Date updateTime = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setUpdateTime(updateTime);
    }
    
    public void LectureNoteDataOnDemand.setUpdatedBy(LectureNote obj, int index) {
        User updatedBy = userDataOnDemand.getRandomUser();
        obj.setUpdatedBy(updatedBy);
    }
    
    public LectureNote LectureNoteDataOnDemand.getSpecificLectureNote(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        LectureNote obj = data.get(index);
        Long id = obj.getId();
        return LectureNote.findLectureNote(id);
    }
    
    public LectureNote LectureNoteDataOnDemand.getRandomLectureNote() {
        init();
        LectureNote obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return LectureNote.findLectureNote(id);
    }
    
    public boolean LectureNoteDataOnDemand.modifyLectureNote(LectureNote obj) {
        return false;
    }
    
    public void LectureNoteDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = LectureNote.findLectureNoteEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'LectureNote' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<LectureNote>();
        for (int i = 0; i < 10; i++) {
            LectureNote obj = getNewTransientLectureNote(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
