// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package tr.edu.metu.ii.sm504.domain;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;
import tr.edu.metu.ii.sm504.domain.Permission;
import tr.edu.metu.ii.sm504.domain.PermissionDataOnDemand;
import tr.edu.metu.ii.sm504.domain.User;
import tr.edu.metu.ii.sm504.domain.UserDataOnDemand;

privileged aspect PermissionDataOnDemand_Roo_DataOnDemand {
    
    declare @type: PermissionDataOnDemand: @Component;
    
    private Random PermissionDataOnDemand.rnd = new SecureRandom();
    
    private List<Permission> PermissionDataOnDemand.data;
    
    @Autowired
    private UserDataOnDemand PermissionDataOnDemand.userDataOnDemand;
    
    public Permission PermissionDataOnDemand.getNewTransientPermission(int index) {
        Permission obj = new Permission();
        setCreatedBy(obj, index);
        setCreationTime(obj, index);
        setName(obj, index);
        setUpdateTime(obj, index);
        setUpdatedBy(obj, index);
        return obj;
    }
    
    public void PermissionDataOnDemand.setCreatedBy(Permission obj, int index) {
        User createdBy = userDataOnDemand.getRandomUser();
        obj.setCreatedBy(createdBy);
    }
    
    public void PermissionDataOnDemand.setCreationTime(Permission obj, int index) {
        Date creationTime = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setCreationTime(creationTime);
    }
    
    public void PermissionDataOnDemand.setName(Permission obj, int index) {
        String name = "name_" + index;
        obj.setName(name);
    }
    
    public void PermissionDataOnDemand.setUpdateTime(Permission obj, int index) {
        Date updateTime = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setUpdateTime(updateTime);
    }
    
    public void PermissionDataOnDemand.setUpdatedBy(Permission obj, int index) {
        User updatedBy = userDataOnDemand.getRandomUser();
        obj.setUpdatedBy(updatedBy);
    }
    
    public Permission PermissionDataOnDemand.getSpecificPermission(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Permission obj = data.get(index);
        Long id = obj.getId();
        return Permission.findPermission(id);
    }
    
    public Permission PermissionDataOnDemand.getRandomPermission() {
        init();
        Permission obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return Permission.findPermission(id);
    }
    
    public boolean PermissionDataOnDemand.modifyPermission(Permission obj) {
        return false;
    }
    
    public void PermissionDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Permission.findPermissionEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Permission' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Permission>();
        for (int i = 0; i < 10; i++) {
            Permission obj = getNewTransientPermission(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
