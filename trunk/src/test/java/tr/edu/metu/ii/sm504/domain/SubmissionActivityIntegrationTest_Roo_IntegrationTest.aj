// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package tr.edu.metu.ii.sm504.domain;

import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;
import tr.edu.metu.ii.sm504.domain.SubmissionActivity;
import tr.edu.metu.ii.sm504.domain.SubmissionActivityDataOnDemand;
import tr.edu.metu.ii.sm504.domain.SubmissionActivityIntegrationTest;

privileged aspect SubmissionActivityIntegrationTest_Roo_IntegrationTest {
    
    declare @type: SubmissionActivityIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: SubmissionActivityIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: SubmissionActivityIntegrationTest: @Transactional;
    
    @Autowired
    private SubmissionActivityDataOnDemand SubmissionActivityIntegrationTest.dod;
    
    @Test
    public void SubmissionActivityIntegrationTest.testCountSubmissionActivitys() {
        Assert.assertNotNull("Data on demand for 'SubmissionActivity' failed to initialize correctly", dod.getRandomSubmissionActivity());
        long count = SubmissionActivity.countSubmissionActivitys();
        Assert.assertTrue("Counter for 'SubmissionActivity' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void SubmissionActivityIntegrationTest.testFindSubmissionActivity() {
        SubmissionActivity obj = dod.getRandomSubmissionActivity();
        Assert.assertNotNull("Data on demand for 'SubmissionActivity' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'SubmissionActivity' failed to provide an identifier", id);
        obj = SubmissionActivity.findSubmissionActivity(id);
        Assert.assertNotNull("Find method for 'SubmissionActivity' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'SubmissionActivity' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void SubmissionActivityIntegrationTest.testFindAllSubmissionActivitys() {
        Assert.assertNotNull("Data on demand for 'SubmissionActivity' failed to initialize correctly", dod.getRandomSubmissionActivity());
        long count = SubmissionActivity.countSubmissionActivitys();
        Assert.assertTrue("Too expensive to perform a find all test for 'SubmissionActivity', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<SubmissionActivity> result = SubmissionActivity.findAllSubmissionActivitys();
        Assert.assertNotNull("Find all method for 'SubmissionActivity' illegally returned null", result);
        Assert.assertTrue("Find all method for 'SubmissionActivity' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void SubmissionActivityIntegrationTest.testFindSubmissionActivityEntries() {
        Assert.assertNotNull("Data on demand for 'SubmissionActivity' failed to initialize correctly", dod.getRandomSubmissionActivity());
        long count = SubmissionActivity.countSubmissionActivitys();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<SubmissionActivity> result = SubmissionActivity.findSubmissionActivityEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'SubmissionActivity' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'SubmissionActivity' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void SubmissionActivityIntegrationTest.testFlush() {
        SubmissionActivity obj = dod.getRandomSubmissionActivity();
        Assert.assertNotNull("Data on demand for 'SubmissionActivity' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'SubmissionActivity' failed to provide an identifier", id);
        obj = SubmissionActivity.findSubmissionActivity(id);
        Assert.assertNotNull("Find method for 'SubmissionActivity' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifySubmissionActivity(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'SubmissionActivity' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void SubmissionActivityIntegrationTest.testMergeUpdate() {
        SubmissionActivity obj = dod.getRandomSubmissionActivity();
        Assert.assertNotNull("Data on demand for 'SubmissionActivity' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'SubmissionActivity' failed to provide an identifier", id);
        obj = SubmissionActivity.findSubmissionActivity(id);
        boolean modified =  dod.modifySubmissionActivity(obj);
        Integer currentVersion = obj.getVersion();
        SubmissionActivity merged = (SubmissionActivity)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'SubmissionActivity' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void SubmissionActivityIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'SubmissionActivity' failed to initialize correctly", dod.getRandomSubmissionActivity());
        SubmissionActivity obj = dod.getNewTransientSubmissionActivity(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'SubmissionActivity' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'SubmissionActivity' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'SubmissionActivity' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void SubmissionActivityIntegrationTest.testRemove() {
        SubmissionActivity obj = dod.getRandomSubmissionActivity();
        Assert.assertNotNull("Data on demand for 'SubmissionActivity' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'SubmissionActivity' failed to provide an identifier", id);
        obj = SubmissionActivity.findSubmissionActivity(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'SubmissionActivity' with identifier '" + id + "'", SubmissionActivity.findSubmissionActivity(id));
    }
    
}
