// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package tr.edu.metu.ii.sm504.domain;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;
import tr.edu.metu.ii.sm504.domain.Semester;
import tr.edu.metu.ii.sm504.domain.SemesterDataOnDemand;
import tr.edu.metu.ii.sm504.domain.User;
import tr.edu.metu.ii.sm504.domain.UserDataOnDemand;

privileged aspect SemesterDataOnDemand_Roo_DataOnDemand {
    
    declare @type: SemesterDataOnDemand: @Component;
    
    private Random SemesterDataOnDemand.rnd = new SecureRandom();
    
    private List<Semester> SemesterDataOnDemand.data;
    
    @Autowired
    private UserDataOnDemand SemesterDataOnDemand.userDataOnDemand;
    
    public Semester SemesterDataOnDemand.getNewTransientSemester(int index) {
        Semester obj = new Semester();
        setCreatedBy(obj, index);
        setCreationTime(obj, index);
        setName(obj, index);
        setUpdateTime(obj, index);
        setUpdatedBy(obj, index);
        setYear(obj, index);
        return obj;
    }
    
    public void SemesterDataOnDemand.setCreatedBy(Semester obj, int index) {
        User createdBy = userDataOnDemand.getRandomUser();
        obj.setCreatedBy(createdBy);
    }
    
    public void SemesterDataOnDemand.setCreationTime(Semester obj, int index) {
        Date creationTime = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setCreationTime(creationTime);
    }
    
    public void SemesterDataOnDemand.setName(Semester obj, int index) {
        String name = "name_" + index;
        obj.setName(name);
    }
    
    public void SemesterDataOnDemand.setUpdateTime(Semester obj, int index) {
        Date updateTime = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setUpdateTime(updateTime);
    }
    
    public void SemesterDataOnDemand.setUpdatedBy(Semester obj, int index) {
        User updatedBy = userDataOnDemand.getRandomUser();
        obj.setUpdatedBy(updatedBy);
    }
    
    public void SemesterDataOnDemand.setYear(Semester obj, int index) {
        Integer year = new Integer(index);
        if (year < 1000 || year > 9999) {
            year = 9999;
        }
        obj.setYear(year);
    }
    
    public Semester SemesterDataOnDemand.getSpecificSemester(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Semester obj = data.get(index);
        Long id = obj.getId();
        return Semester.findSemester(id);
    }
    
    public Semester SemesterDataOnDemand.getRandomSemester() {
        init();
        Semester obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return Semester.findSemester(id);
    }
    
    public boolean SemesterDataOnDemand.modifySemester(Semester obj) {
        return false;
    }
    
    public void SemesterDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Semester.findSemesterEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Semester' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Semester>();
        for (int i = 0; i < 10; i++) {
            Semester obj = getNewTransientSemester(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
