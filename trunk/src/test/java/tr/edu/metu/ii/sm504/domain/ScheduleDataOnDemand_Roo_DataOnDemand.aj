// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package tr.edu.metu.ii.sm504.domain;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;
import tr.edu.metu.ii.sm504.domain.Schedule;
import tr.edu.metu.ii.sm504.domain.ScheduleDataOnDemand;
import tr.edu.metu.ii.sm504.domain.ScheduleTemplate;
import tr.edu.metu.ii.sm504.domain.ScheduleTemplateDataOnDemand;
import tr.edu.metu.ii.sm504.domain.Semester;
import tr.edu.metu.ii.sm504.domain.SemesterDataOnDemand;
import tr.edu.metu.ii.sm504.domain.User;
import tr.edu.metu.ii.sm504.domain.UserDataOnDemand;

privileged aspect ScheduleDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ScheduleDataOnDemand: @Component;
    
    private Random ScheduleDataOnDemand.rnd = new SecureRandom();
    
    private List<Schedule> ScheduleDataOnDemand.data;
    
    @Autowired
    private UserDataOnDemand ScheduleDataOnDemand.userDataOnDemand;
    
    @Autowired
    private ScheduleTemplateDataOnDemand ScheduleDataOnDemand.scheduleTemplateDataOnDemand;
    
    @Autowired
    private SemesterDataOnDemand ScheduleDataOnDemand.semesterDataOnDemand;
    
    public Schedule ScheduleDataOnDemand.getNewTransientSchedule(int index) {
        Schedule obj = new Schedule();
        setCreatedBy(obj, index);
        setCreationTime(obj, index);
        setScheduleTemplate(obj, index);
        setSemester(obj, index);
        setUpdateTime(obj, index);
        setUpdatedBy(obj, index);
        return obj;
    }
    
    public void ScheduleDataOnDemand.setCreatedBy(Schedule obj, int index) {
        User createdBy = userDataOnDemand.getRandomUser();
        obj.setCreatedBy(createdBy);
    }
    
    public void ScheduleDataOnDemand.setCreationTime(Schedule obj, int index) {
        Date creationTime = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setCreationTime(creationTime);
    }
    
    public void ScheduleDataOnDemand.setScheduleTemplate(Schedule obj, int index) {
        ScheduleTemplate scheduleTemplate = scheduleTemplateDataOnDemand.getRandomScheduleTemplate();
        obj.setScheduleTemplate(scheduleTemplate);
    }
    
    public void ScheduleDataOnDemand.setSemester(Schedule obj, int index) {
        Semester semester = semesterDataOnDemand.getRandomSemester();
        obj.setSemester(semester);
    }
    
    public void ScheduleDataOnDemand.setUpdateTime(Schedule obj, int index) {
        Date updateTime = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setUpdateTime(updateTime);
    }
    
    public void ScheduleDataOnDemand.setUpdatedBy(Schedule obj, int index) {
        User updatedBy = userDataOnDemand.getRandomUser();
        obj.setUpdatedBy(updatedBy);
    }
    
    public Schedule ScheduleDataOnDemand.getSpecificSchedule(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Schedule obj = data.get(index);
        Long id = obj.getId();
        return Schedule.findSchedule(id);
    }
    
    public Schedule ScheduleDataOnDemand.getRandomSchedule() {
        init();
        Schedule obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return Schedule.findSchedule(id);
    }
    
    public boolean ScheduleDataOnDemand.modifySchedule(Schedule obj) {
        return false;
    }
    
    public void ScheduleDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Schedule.findScheduleEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Schedule' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Schedule>();
        for (int i = 0; i < 10; i++) {
            Schedule obj = getNewTransientSchedule(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
