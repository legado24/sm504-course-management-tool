// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package tr.edu.metu.ii.sm504.domain;

import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;
import tr.edu.metu.ii.sm504.domain.Semester;
import tr.edu.metu.ii.sm504.domain.SemesterDataOnDemand;
import tr.edu.metu.ii.sm504.domain.SemesterIntegrationTest;

privileged aspect SemesterIntegrationTest_Roo_IntegrationTest {
    
    declare @type: SemesterIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: SemesterIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: SemesterIntegrationTest: @Transactional;
    
    @Autowired
    private SemesterDataOnDemand SemesterIntegrationTest.dod;
    
    @Test
    public void SemesterIntegrationTest.testCountSemesters() {
        Assert.assertNotNull("Data on demand for 'Semester' failed to initialize correctly", dod.getRandomSemester());
        long count = Semester.countSemesters();
        Assert.assertTrue("Counter for 'Semester' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void SemesterIntegrationTest.testFindSemester() {
        Semester obj = dod.getRandomSemester();
        Assert.assertNotNull("Data on demand for 'Semester' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Semester' failed to provide an identifier", id);
        obj = Semester.findSemester(id);
        Assert.assertNotNull("Find method for 'Semester' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Semester' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void SemesterIntegrationTest.testFindAllSemesters() {
        Assert.assertNotNull("Data on demand for 'Semester' failed to initialize correctly", dod.getRandomSemester());
        long count = Semester.countSemesters();
        Assert.assertTrue("Too expensive to perform a find all test for 'Semester', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Semester> result = Semester.findAllSemesters();
        Assert.assertNotNull("Find all method for 'Semester' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Semester' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void SemesterIntegrationTest.testFindSemesterEntries() {
        Assert.assertNotNull("Data on demand for 'Semester' failed to initialize correctly", dod.getRandomSemester());
        long count = Semester.countSemesters();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Semester> result = Semester.findSemesterEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Semester' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Semester' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void SemesterIntegrationTest.testFlush() {
        Semester obj = dod.getRandomSemester();
        Assert.assertNotNull("Data on demand for 'Semester' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Semester' failed to provide an identifier", id);
        obj = Semester.findSemester(id);
        Assert.assertNotNull("Find method for 'Semester' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifySemester(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'Semester' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void SemesterIntegrationTest.testMergeUpdate() {
        Semester obj = dod.getRandomSemester();
        Assert.assertNotNull("Data on demand for 'Semester' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Semester' failed to provide an identifier", id);
        obj = Semester.findSemester(id);
        boolean modified =  dod.modifySemester(obj);
        Integer currentVersion = obj.getVersion();
        Semester merged = (Semester)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'Semester' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void SemesterIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Semester' failed to initialize correctly", dod.getRandomSemester());
        Semester obj = dod.getNewTransientSemester(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Semester' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Semester' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Semester' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void SemesterIntegrationTest.testRemove() {
        Semester obj = dod.getRandomSemester();
        Assert.assertNotNull("Data on demand for 'Semester' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Semester' failed to provide an identifier", id);
        obj = Semester.findSemester(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Semester' with identifier '" + id + "'", Semester.findSemester(id));
    }
    
}
